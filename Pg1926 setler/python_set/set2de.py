def kontrol(str):  
    bölünmüş = mailAdresi.split("@")
    for ch in bölünmüş :
        if():
    



for a in range (3):
    if a==0:
        kullanıcıAdı = "Gaziuniversitesi"
        websiteSağlayıcısı = "gazi"
        siteUzantısı = ".edu"
        ülkeKodu = ".tr"
        mailAdresi = kullanıcıAdı + "@" + websiteSağlayıcısı + siteUzantısı + ülkeKodu
        uzunluk = 3
    if a==1:
        kullanıcıAdı = "Gaziuniversitesi"
        websiteSağlayıcısı = "gazi"
        siteUzantısı = ".edu"
        ülkeKodu = ".tr"
        mailAdresi = kullanıcıAdı + "." + websiteSağlayıcısı + siteUzantısı + ülkeKodu
        uzunluk = 2
    if a==2:
        kullanıcıAdı = "Gaziuniversitesi"
        websiteSağlayıcısı = "gazi"
        siteUzantısı = ".edu"
        ülkeKodu = ".tr"
        mailAdresi = kullanıcıAdı + "." + websiteSağlayıcısı + siteUzantısı 
        uzunluk = 3
        
for a in range(3):
    if(kontrol(mailAdresi)==True):
        print(uzunluk)
        print(mailAdresi)
        print('Mail adresiniz doğrudur.')
    else:
        print(uzunluk)
        print(mailAdresi)
        print('Mail adresiniz yanlıştır')
 